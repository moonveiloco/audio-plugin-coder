{
  "canonical_dir": "agents",
  "comment": "Single source of truth = agents/. Hidden homes contain only pointers generated by scripts/sync-agent-pointers.sh|.ps1. To add a new agent (e.g. .cursor): add an entry to homes with the flags for what the tool loads, then re-run the sync.",
  "homes": [
    {
      "dir": ".claude",
      "rules": false,
      "skills": true,
      "workflows": true
    },
    {
      "dir": ".kilocode",
      "rules": true,
      "skills": true,
      "workflows": true
    }
  ]
}
