---
name: setup
description: "Setup & onboarding (first run). Trigger: /setup, or automatically when the APC config is missing or setup_complete=false. Guides through the plugins directory choice and saves ~/.config/apc/config.json."
---

# Skill: setup (pointer)

Load and run **exactly** `agents/skills/setup/SKILL.md` (single source of truth). Do not improvise the skill content.
