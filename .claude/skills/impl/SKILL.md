---
name: impl
description: "PHASE 4: Implementation - DSP and UI code. Use when the user types /impl [Name]. Implements PluginProcessor, binds parameters to UI, builds via scripts/build-and-install.sh|.ps1."
---

# Skill: impl (pointer)

Load and run **exactly** `agents/skills/impl/SKILL.md` (single source of truth). Do not improvise the skill content.
