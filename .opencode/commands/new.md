---
description: "APC: full plugin development from idea to release (with confirmation at each phase)"
---
Load and run **exactly** `agents/workflows/new.md` (single source of truth).
If the plugin name is not given in $ARGUMENTS, ask the user before proceeding.
Each phase requires the user's approval before moving to the next one.

Plugin: $ARGUMENTS